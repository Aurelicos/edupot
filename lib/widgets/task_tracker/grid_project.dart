import 'package:flutter/material.dart';

class GridProject extends StatelessWidget {
  const GridProject({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
